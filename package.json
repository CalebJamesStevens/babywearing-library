{
  "name": "babywearing-library",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "dev:all": "node scripts/dev-all.mjs",
    "dev:web": "npm -C apps/web run dev",
    "dev:admin": "npm -C apps/admin run dev",
    "build": "npm -C apps/admin run build && npm -C apps/web run build",
    "build:web": "npm -C apps/web run build",
    "build:admin": "npm -C apps/admin run build",
    "lint": "npm -C apps/admin run lint && npm -C apps/web run lint",
    "db:generate": "npm -C packages/db run generate",
    "db:migrate": "npm -C packages/db run migrate",
    "db:studio": "npm -C packages/db run studio"
  },
  "dependencies": {
    "@neondatabase/auth": "^0.2.0-beta.1",
    "ua-parser-js": "2.0.9"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
